node_modules
.env
.DS_Store
.yarn

# Hardhat and circom files
cache
circuits/output
coverage.json
coverage
contracts/
artifacts/*

# powers of tau file (circom files from the trusted setup ceremony)
*.ptau

# Typechain generated files
generated-types

# Keep all output, smart contract, and artifact files related to the `division` circuit
# to have one example of how correct compilation and contracts should look.
!artifacts/circom
artifacts/circom/*
!artifacts/circom/division.*
!circuits/output/division.*
!contracts/DivisionVerifier.sol


